import { Meta, Story } from '@storybook/addon-docs/blocks';
import Radio from './Radio';

<Meta title='UI / Forms / Radio' component={Radio} />

export const Template = args => <Radio {...args} />;

<Story name='Default'>{Template.bind({})}</Story>
